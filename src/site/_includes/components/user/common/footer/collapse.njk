<script>
document.addEventListener('DOMContentLoaded', () => {
    // We use a small delay to ensure the Digital Garden has finished rendering
    setTimeout(() => {
        const headings = document.querySelectorAll('.content h2, .content h3');

        headings.forEach(heading => {
            heading.style.cursor = 'pointer';

            // Add the chevron indicator
            if (!heading.querySelector('.toggle-icon')) {
                heading.insertAdjacentHTML('afterbegin', '<span class="toggle-icon" style="display:inline-block; transition: transform 0.2s; margin-right:10px; opacity:0.5;">▶</span>');
            }

            heading.addEventListener('click', function() {
                const icon = this.querySelector('.toggle-icon');

                // Toggle the icon rotation
                if (icon) {
                    const isExpanded = icon.style.transform === 'rotate(90deg)';
                    icon.style.transform = isExpanded ? 'rotate(0deg)' : 'rotate(90deg)';
                }

                // Toggle siblings until the next heading
                let next = this.nextElementSibling;
                while (next && !['H1', 'H2', 'H3'].includes(next.tagName)) {
                    next.style.display = (next.style.display === 'none') ? 'block' : 'none';
                    next = next.nextElementSibling;
                }
            });
        });
    }, 500);
});
</script>
